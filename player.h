#ifndef PLAYER_H
#define PLAYER_H

#include "song.h"

void startPlaying(struct Player* player);
void nextSong(struct Player* player);
void prevSong(struct Player* player);
void stopPlaying(struct Player* player);

// shuffle modes

#endif 